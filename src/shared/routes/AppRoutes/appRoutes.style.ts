import { StyleSheet } from "react-native";

export const TabStyles = (theme: any) => (
    StyleSheet.create({
        image: {
            top: 7,
            width: 40,
            height: 40,
            borderRadius: 50 / 2,
            marginRight: "4%",
            paddingBottom: -20
        }
    })
)